# Local roles and customizations

Use this directory for thin wrappers, overrides, or experimental roles that complement the upstream OpenHPC role.

## TODO

- Keep local roles small and focused.
- Prefer extending the upstream role cleanly instead of duplicating it wholesale.
