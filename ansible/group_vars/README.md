# Group variables

Store shared variables here for inventory groups such as controllers, GPU workers, and optional cloud workers.

## TODO

- Add non-sensitive defaults only.
- Keep secrets in a separate encrypted workflow rather than committing them here.
